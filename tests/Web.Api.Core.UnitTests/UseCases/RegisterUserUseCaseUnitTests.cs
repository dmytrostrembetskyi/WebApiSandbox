using Web.Api.Core.Dto.UseCaseRequests;
using Xunit;

namespace WebApiSandbox.Api.Core.UnitTests.UseCases
{
    public class RegisterUserUseCaseUnitTests
    {
        [Fact]
        public async void Can_Register_User()
        {
            // arrange
            // var userRepository = new UserReposotory();
            // var userCase = new RegisterUserUseCase(userRepository);
            // var outputPort = new OutputPort();
            //
            // // act
            // var response =
            //     await userCase.Handle(
            //         new RegisterUserRequest("firstName", "lastName", "email@domain.com", "userName", "password"),
            //         outputPort);
            //
            // // assert
            // Assert.True(response);
        }
    }
}